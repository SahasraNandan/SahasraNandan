package com.core.java.classandobjects;

public class Polymorphism {


    //many behaviour means one method act as may operation
    // polymorphism is two types one is compiletime(method loading)
    // Two is runTime(method Overriding)

    // Method loading in complile time we know which method is run
    // Method overrding means inheritance it will act as based the object creation in
    // run time
}
